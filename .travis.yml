language: r
cache: packages

warnings_are_errors: true

os:
  - linux
  - osx

r:
  - 3.8
  - 3.9
  - 4.0
  - release
  - devel

after_success:
  - Rscript -e 'covr::codecov(type = "all")'

